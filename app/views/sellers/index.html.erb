       <!-- Main content -->
        <section class="content">
          <%= render 'sellers_table' %>
        </section><!-- /.content -->


 <script>
      $(function () {
        $("#sellers_table").DataTable({
          "language": {
            "url": "/plugins/datatables/dataTables.spanish.lang"
          },
          "columnDefs": [ {
          "targets": 'no-sort',
          "orderable": false,
          "searchable": false
          } ],
          stateSave: true
        });
      });
</script>
<br>

<%= link_to 'Nuevo  Vendedor', current_user.admin? ? new_seller_path : new_seller_of_commerce_path(@commerce) ,class:"visible-xs btn btn-block btn-primary btn-lg"%>
<%= link_to 'Nuevo  Vendedor', current_user.admin? ? new_seller_path : new_seller_of_commerce_path(@commerce) ,class:"hidden-xs btn btn-primary "%>

