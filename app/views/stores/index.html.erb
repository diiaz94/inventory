       <!-- Main content -->
        <section class="content">
          <%= render 'stores_table' %>
        </section><!-- /.content -->


 <script>
      $(function () {
        $("#stores_table").DataTable({
          "language": {
            "url": "/plugins/datatables/dataTables.spanish.lang"
          },
          "columnDefs": [ {
          "targets": 'no-sort',
          "orderable": false,
          "searchable": false
          } ],
          stateSave: true
        });
      });
</script>
<br>

<%= link_to 'Nueva  Tienda', current_user.admin? ? new_store_path : new_store_of_commerce_path(@commerce)  ,class:"visible-xs btn btn-block btn-primary btn-lg"%>
<%= link_to 'Nueva  Tienda', current_user.admin? ? new_store_path : new_store_of_commerce_path(@commerce)  ,class:"hidden-xs btn btn-primary "%>

